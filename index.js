function arrayBuilder(one, two, three) {
    var arr = [];
    arr.push(one);
    arr.push(two);
    arr.push(three);
    return arr;
}

let FirstArrayByFunction = arrayBuilder("Geeta", "sita", 23);
console.log(some);