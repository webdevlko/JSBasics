// Generate random number between 0 and 0.99

Math.random();

// Save it to a varible
let decimal = Math.random();

// Log the value of decimal to the console
console.log(decimal);

// Log the value of decimal multiplyed by 10
console.log(decimal * 10);